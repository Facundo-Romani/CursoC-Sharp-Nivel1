using System;

namespace ejemplo_condicionales
{
    class Program
    {
        static void Main(string[] args)
        {
            // OPERADOR AND &&
            // TRUE && TRUE = TRUE
            // FALSE && TRUE = FALSE
            // TRUE && FALSE = FALSE
            // FALSE && FALSE = FALSE

            // OPERADOR OR  ||
            // TRUE && TRUE = TRUE
            // FALSE && TRUE = TRUE
            // TRUE && FALSE = TRUE
            // FALSE && FALSE = FALSE

            // OPERADOTR NOR ! NEGAR CAMBIA LA CONDICIÓN

            Console.WriteLine("Hello World!");
        }
    }
}
